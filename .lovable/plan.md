## שינויים ב-`src/routes/_authenticated/chat.$threadId.tsx`

שלוש עריכות בעמוד הצ'אט (מסך פתיחה של שיחה חדשה):

### 1. מחיקת תיאור (קו אדום)
מחיקת הפסקה בשורות 366-368:
```
תאר/י במילים שלך את מה שצריך וה-AI יבנה לך {מסמך/תרשים}.
```

### 2. כותרת חדשה (הקפה כחולה) — שורה 365
במקום `{def?.label}` יוצג:
```
אני סוכן AI מומחה לכתיבת {def?.label}
```
כאשר `def?.label` הוא שם סוג המסמך שהמשתמש בחר (למשל "מסמך אפיון", "תרשים זרימה" וכו').

### 3. Placeholder חדש בתיבת הכתיבה (הקפה צהובה) — שורה 483
במקום:
```
תאר/י את ה{def?.label}...
```
יוצג:
```
תאר את ה{def?.label} שתרצה ליצור...
```

(שני המקומות ממשיכים להשתמש ב-`def?.label` כך שהשם מתאים לסוג שהמשתמש בחר.)

אין שינויים לוגיים, סגנוניים או backend — רק טקסט.
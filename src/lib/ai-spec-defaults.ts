// Client-safe stub for AI spec defaults.
// All prompts, model names, and schema descriptions live in
// `ai-spec-defaults.server.ts` so they never leak into the browser bundle.

export type SpecModel = string;
